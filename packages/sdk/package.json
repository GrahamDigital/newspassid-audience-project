{
  "name": "@newspassid-audience/sdk",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/newspassid.js",
  "module": "dist/newspassid.esm.js",
  "types": "dist/newspassid.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:async-loader": "vite build --mode async",
    "build:library": "vite build",
    "dev": "vite serve",
    "build": "pnpm run build:library && pnpm run build:async-loader && pnpm run build:examples",
    "build:examples": "vite build --mode examples",
    "predev": "pnpm run build"
  },
  "dependencies": {
    "@newspassid-audience/core": "workspace:*",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "vite": "^5.0.0"
  }
}
